<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	android:id="@+id/activity_main"
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:paddingBottom="@dimen/activity_vertical_margin"
	android:paddingLeft="@dimen/activity_horizontal_margin"
	android:paddingRight="@dimen/activity_horizontal_margin"
	android:paddingTop="@dimen/activity_vertical_margin"
	tools:context="com.luke.quizparagraph.QuizActivity">

	<EditText
		android:id="@+id/edit_input_phrase_main"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:hint="@string/input_phrase"/>

	<Button
		android:id="@+id/btn_add_phrase_main"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_below="@id/edit_input_phrase_main"
		android:text="@string/add_phrase"/>

	<RelativeLayout
		android:id="@+id/layout_paragraph_main"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_below="@id/btn_add_phrase_main"
		android:layout_margin="0dp"
		android:padding="0dp">
	</RelativeLayout>
</RelativeLayout>
